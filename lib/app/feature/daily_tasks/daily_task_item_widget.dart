import 'package:flutter/material.dart';

class DailyTaskItemWidget extends StatelessWidget {
  const DailyTaskItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
